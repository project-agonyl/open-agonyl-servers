package constants

const ServerUnderMaintenanceMsg = "Server is under maintenance!"

const AccountAlreadyLoggedInMsg = "Account already logged in!"

const AccountNotActiveMsg = "Account is not active!"

const AccountBannedMsg = "Account is banned!"

const InvalidCredentialsMsg = "Invalid credentials!"
